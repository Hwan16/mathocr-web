import { NextResponse } from "next/server";

// ── 관리자: 얼리버드 오픈 안내 메일 발송 — 종료 (2026-07-12 비활성화, 사용자 결정) ──
//
// 이력: 신청제(0015)의 '오픈 날 딸깍' 버튼이었으나, 신청자 0명 상태로 신청제가
// 종료되어(2026-07-11 가입 직결 개편) 발송 대상이 없다. 메일 본문의 지급 안내
// ("가입하면 총 30문제")도 현행 '이메일 인증 후 지급' 정책과 어긋나므로, 실수로
// 발송되는 일이 없게 완전히 닫는다 (Codex 리뷰 LA-02 후속).
// 이전 구현(배치 발송·수신거부 토큰·dry run)이 필요하면 git 이력(web 2899c57
// 이전) 참조 — 재사용 시 반드시 문구를 새 정책 기준으로 다시 쓸 것.

export async function POST() {
  return NextResponse.json(
    {
      error:
        "얼리버드 사전 신청 안내 메일 기능은 종료되었습니다 (신청제 폐기, 대상 0명). 코드가 필요하면 git 이력을 참조하세요.",
    },
    { status: 410 }
  );
}
